---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ssh-config
  namespace: cert-manager
data:
  config: |-
    Host github.com
      StrictHostKeyChecking no
    Host udmp.${DOMAIN}
      StrictHostKeyChecking no
