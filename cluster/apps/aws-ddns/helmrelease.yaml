---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: aws-ddns
  namespace: aws-ddns
spec:
  interval: 1h
  maxHistory: 3

  install:
    createNamespace: true
    remediation:
      retries: 3

  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3

  uninstall:
    keepHistory: false

  chart:
    spec:
      # renovate: registryUrl=https://bjw-s-labs.github.io/helm-charts
      chart: app-template
      version: 3.7.3
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system

  values:
    controllers:
      main:
        type: cronjob
        cronjob:
          concurrencyPolicy: Forbid
          schedule: "*/15 * * * *"
          successfulJobsHistory: 8
        
        pod:
          restartPolicy: OnFailure
        
        containers:
          main:
            image:
              repository: public.ecr.aws/hashicorp/terraform
              tag: 1.14.2@sha256:eee2f7d5725bfcfd734dfc9fe5a3df4b58b00eb8cc874993458108d8943265cf
            
            command:
              - /bin/sh
              - /app/aws-ddns.sh
            
            envFrom:
              - secretRef:
                  name: aws-ddns-secrets

    service:
      main:
        controller: main
        enabled: false

    persistence:
      config:
        type: configMap
        name: aws-ddns-configmap
        defaultMode: 0775
        globalMounts:
          - path: /app/aws-ddns.sh
            subPath: aws-ddns.sh
            readOnly: true
      terraform:
        type: configMap
        name: tf-configmap
        defaultMode: 0644
        globalMounts:
          - path: /app/main.tf
            subPath: main.tf
            readOnly: true
      backend:
        type: secret
        name: aws-ddns-tf-backend
        globalMounts:
          - path: /app/backend.tf
            subPath: backend.tf
            readOnly: true
