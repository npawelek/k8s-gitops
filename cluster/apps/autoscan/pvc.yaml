---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-autoscan-0
  namespace: autoscan
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
