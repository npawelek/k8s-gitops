---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptime-data
  namespace: uptime
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
