---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-plex-0
  namespace: plex
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi
  storageClassName: longhorn
